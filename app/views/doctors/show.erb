<h3><%=@doctor.name%></h3>

<h5>Appointments:</h5>
<ul>
<% @doctor.appointments.each do |a| %>
    <%=link_to a.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(a.id)%>
    <%=link_to a.patient.name, patient_path(a.patient)%>
<% end %>
</ul>